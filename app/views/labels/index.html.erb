
    <div class="d-flex flex-column align-items-center mt-3">
      <button class="btn btn-outline-dark rounded-pill mt-5">
        <%= link_to 'Create a New Label', new_label_path %>
      </button>
      <br>
      <br>

    <div class="col-xl-7 col-lg-8 col-md-10 col-sm-11 post-card">

    <div class="card">


      <div class="card-header">
        All labels
      </div>
      <div class="card-body">

<table class = "table table-bordered ">
  <thead class="bg-dark text-light">
    <tr>
      <th>Label Name</th>
      <th>User</th>
      <th colspan="3">More Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @labels.each do |label| %>
      <tr>
        <td><%= label.name %></td>
        <td><%= label.user.firstname %></td>
        <td>
        <button class="btn  rounded-pill"><%= link_to 'Show', label %></button>
        <button class="btn  rounded-pill"><%= link_to 'Edit', edit_label_path(label) %></button>
        <button class="btn rounded-pill "><%= link_to 'Delete', label, method: :delete, data: { confirm: 'Are you sure?' } %></button>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
</div>
</div>
